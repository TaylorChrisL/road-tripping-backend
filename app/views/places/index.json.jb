@places.map do |place|
  {
  id: place.id,
  name: place.name,
  address: place.address,
  city: place.city,
  zip_code: place.zip_code,
  longitude: place.longitude,
  latitude: place.latitude
  }
end
